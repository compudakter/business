<div class="steps">
  <div class="container steps__container">
   {{#each steps}}
       {{>../../ui/step/index.handlebars block="steps" variant=variant title=title text=text icon=icon}}  
   {{/each}}
  </div>
</div>